const matchRides = (requests) => {
  console.log("Matching logic running for", requests.length, "requests");
  return requests; // Basic version - can be enhanced later
};

module.exports = { matchRides };